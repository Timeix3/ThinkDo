# Backend

## 🧹 Форматирование кода Backend
Мы используем стандартный линтер .NET. Чтобы ваш код прошел проверку в CI/CD, перед коммитом выполните команду в папке `server/`:

```bash
dotnet format server/server.sln
